package oracle;

public interface ListenInterface {

    void Listen(Oracle.needs myNeeds);
    void ListenA(Oracle.needs myNeeds);
    void ListenM(Oracle.needs myNeeds);
    void ListenP(Oracle.needs myNeeds);
}
